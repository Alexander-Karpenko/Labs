#pragma once
#include <iostream>
#include <fstream>
#include <vector>
#include <string>

using namespace std;
void fin1();
void outfile1(bool& error);
int inK(bool& error);
vector<string> delete12(int k, vector<string> lines);